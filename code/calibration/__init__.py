"""Ticket 14: the calibration block's sweep harness.

Not part of the submission's decision path. `code/main.py` never imports this package;
it exists so the unfrozen `Config` parameters are settled by measurement against the 25
solved samples rather than by argument, and so the measurement is reproducible.
"""
